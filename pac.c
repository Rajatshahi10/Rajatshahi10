#include <stdio.h>
int main()
{
    int a[10], size, i, pos, insert;
    printf("enter the size of array :");
    scanf("%d", &size);
    printf("inserted value is :");
    scanf("%d", &insert);
    printf("enter the position for inserted value :");
    scanf("%d",&pos);
    a[0]=insert;
    printf("array after insertion :");
    for(i=0;i<=0;i++){
        printf("%d ",a[i]);
    }
        return 0;
}